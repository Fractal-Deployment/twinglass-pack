---
name: llmve-translate
description: >
  LLMVE translate (not LCD). After the LCD lens has named collection vs
  demand, put the math they actually collected onto an LLMVE pathway
  (Mythos-evidence, substrate-friction question). Do not import their claim.
when-to-use: llmve-translate, LMVE translate, reuse collected math onto Mythos or friction
user-invocable: true
metadata:
  short-description: "Collected math onto an LLMVE pathway"
---

# llmve-translate

**One job:** take the function they actually ran and put it on an **LLMVE** pathway. Not LCD. LCD only looks.

`train_ok=false · measured_omega=false · G1=OPEN`

- Toward **Mythos-evidence** — if they collected a geometry snapshot. Not a Mythos number. Not \(P_L\).
- Toward **substrate friction / why steer** — as a question. Steer does not rewrite Mythos (`llmve-meaning`). Do not implement ΔW.

Meaning lock: `llmve-meaning`. Compute: `llmve-factor-compute` (dumps only).

Grok.com: /order (translate block). CLI: this folder.
