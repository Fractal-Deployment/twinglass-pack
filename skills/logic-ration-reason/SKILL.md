---
name: logic-ration-reason
description: >
  Standing Logic · Ration · Reason lock. Three laws, markers from residue,
  claims only with necessary_because AND a marker. Telos does not move.
  Not a gait. Not value ranking. Not glossary LCD join. Not a rewrite
  folder that also owns the laws.
when-to-use: LRR, logic ration reason, three laws, mint markers, necessary_because, keep telos, contract-lrr
user-invocable: true
metadata:
  short-description: "Logic · Ration · Reason. Telos stays."
---

# logic-ration-reason

**One job:** hold Logic, Ration, and Reason as the standing cognition lock. Every other pack skill consumes this. None of them restate it as original.

`measured_omega=false` · no invent-green

Load this on the **research / thinking agent**. That agent checks whether the argumentation path is self-consistent. Monitor is SI + LCD only. Do not put this skill on the sentinel.



## Logic

Three laws. Always.

1. **Identity** — a locked term keeps one referent. New sense, new name.
2. **Non-contradiction** — do not hold A and not-A under the same lock.
3. **Excluded middle** — do not hide the split in a vague third.

If two internally coherent accounts cannot both be true under the lock, that is an exclusive split. Specialization (`usedAs ≤ lockedDef`) is not a split. Synonyms are not a split.

## Ration

Mint short pattern markers from residue. Do not narrate residue away.

A marker names a recurring structure on the working material (a dump, a charge, a skill job, a claim package). Markers do not stamp leaves. Markers do not close Ω.

Examples of legal markers (pattern only): `identity_rotating`, `late_cut_only`, `packing_illusion`, `silent_rename`, `second_copy_of_job`.

## Reason

Keep a claim only with **both**:

- `necessary_because` — a telos link (why this claim serves the charge), and
- a marker — residue that the claim is answering.

Telos does not move. The working prompt may. Fluency is not a warrant.

## When the research agent has left a rational path

Do **not** inverse CoT. Debugging by walking backwards from the bad conclusion is not LRR.

**Forward crawl:** from the first claim to the first step that fails Logic or Reason (NC on the string, missing `necessary_because`, missing marker). That index is the diverge. Rebuild CoT from the consistent prefix. Then hand to the gait (fork only if exclusive accounts).

Do not paste Pearl’s three rules. If seeing a sentence is doing the meter, identity already failed. Markers are residue patterns, never seals (`measured_omega=false` is not a marker). “Keep” means keep the lock, not the paper. Paper vs lock exclusive → crawl from the start, do not say “no diverge.”



## Each round (research agent)

1. Read the lock. Do not rewrite the laws.
2. Logic — same term, same referent.
3. Ration — mint or reuse markers. Do not bury them in prose.
4. Reason — drop claims that lack `necessary_because` ∧ marker.
5. Hand the contracted prompt to the gait that owns the geometry (lattice, twin, tournament). Do not become that gait.

## Load / refuse

**Load this** from: `honest-prompt-rewrite`, `twinglass-lattice`, `twinglass-twin`, `steelman-truth-tournament`, `/activate`.

**Do not load this as a substitute for:**

| Skill | Why it is a different job |
|---|---|
| `lcd-glossary-integrity` | SI on load-bearing words. Not this lock. |
| `reason-telos-lookup` | Value ranking and friend–enemy. LRR does not say what may be sacrificed. |
| `llmve-meaning` | Symbol jurisdiction for meters. LRR does not define \(P_L\). |
| `twinglass-lattice` | Earned forks. LRR does not scout. |
| `twinglass-twin` | Known gutter. LRR does not split tracks. |
| `honest-prompt-rewrite` | Applies this lock to a working prompt. Does not own the laws. |

## Do not

- Close `measured_omega` from a fluent paragraph.
- Copy this folder as `morph-shared`. Collation is `collate-hourglass` and does not own these laws.
- Load this onto the integrity monitor as extra objects. The monitor judges two objects (terms, quantities) **by** the three laws. Ration and Reason stay here. Laws-as-a-third-watch-object overloads and refuses.
- Become meaning, LCD, telos-ranking, lattice, or twin.
- Invent a fourth law.

Grok.com: Forge (this lock) and Twin lock phase. CLI: this folder. `/activate` loads this, then rewrite.
