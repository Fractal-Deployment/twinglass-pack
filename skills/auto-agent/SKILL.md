---
name: auto-agent
description: Outer telos-marker / CI-watch loop. Not meet. Not lattice. Not laws.
---

# auto-agent

**One job:** outer loop — SELECT → gates → PR → CI watch → classify. Do not merge.

Occupant = outer loop only. Consume `commands/agent5-next.md` for SELECT. Consume `skills/twinglass-lattice` for earned diverge. Consume `skills/collate-hourglass` for meet. Do not restate laws (LRR). Do not claim DEBATE or SYNTHESIS.

Telos = `queue/telos.json`. Failures regain the next unmet marker.

T3 ≠ Zed. `measured_omega=false`.
