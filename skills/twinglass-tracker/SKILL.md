---
name: twinglass-tracker
description: >
  Semantic integrity as a gather ledger. Close debts when the pad fills them.
  Stagger meet until axes are named. Do not wipe the pad. Do not send the
  walker back. Not the walker. Not the meet.
when-to-use: SI ledger, gather, stagger meet, coordinate map, open debts
user-invocable: true
metadata:
  short-description: "SI gather ledger; stagger until named"
---
# twinglass-tracker
**One job:** keep SI developing. Gather. Do not smack the walker back to a previous point.

Load `lcd-glossary-integrity` (terms) and `lcd-lens` (functions). Dual cap unchanged. This skill is the **ledger**, not a third watch object.

## Debts (close, do not reopen if still present)
| Axis | Needed |
|---|---|
| event-plane | Name the plane this pad is on. |
| g-definition | What G names here. |
| energy | Vector, scalar, or a dead coordinate. Keep the vector until a coordinate dies. |
| path | Find → Maintain → Let-fail. Not hold a token. |
| units | Countable units for a later join. |
| locks | One name, one referent. |
| residue | Quote. Do not narrate away. |

Merge pads: union locks, markers, claims, units, tags. Newer named axis wins. Empty does not overwrite full.

## Stagger
If a required axis is unnamed: **stagger**. Keep pads. Keep walking. Do not contract.
If both planes are **named** but different (PAIR gutter): protocol synthesis — hand to `collate-hourglass`. Not a reset.
If units join: collate may debate or synthesize. Exclusive leftover after collate → `steelman-truth-tournament`. This skill does not run that meet.

## Do not
- Wipe the pad to “go back.”
- Own DEBATE or SYNTHESIS.
- Watch a third monitor object (overload refuse stays on SI + LCD).
- Compute F, T_tail, P_L (`llmve-factor-compute` HOLD).
Grok.com: SI panel. CLI: this folder.
