# Condition P board — H0 named

`measured_omega=false` · no invent-green


**H0.** On Grok Build CLI desktop, interrupt-hibernate is a **scheduler**. Isolation that held was **no sibling pad / URLs / quotes**, not one-awake.

TRUE_PARALLEL=yes. Two child sessions, `context_source=new`, start 4 ms apart. Jaccard 0 at meet. sibling_read=false. paraphrase=no. SHARED_TRANSCRIPT=no. MINTED_DUMP_METER=false.

Condition H (paces, sequential awake) is a **different board**. Do not cite it as H0 or H1.

Lattice **default is not rewritten here.** Next ops charge may set CLI default to `schedule=parallel` and keep interrupt for grok.com. Parent seeing both pads at meet is **collate**, not a shared research transcript.

Plugin `2fecf99d` is still old. Reinstall from `main`.
