{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "read_file|grep|Read|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "${GROK_PLUGIN_ROOT}/hooks/deny-session-read.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
