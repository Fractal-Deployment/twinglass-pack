# S08 — Async equator paths

**other-track:** Per-path pendingProjection already in engine — CLI/docs must match.

## Do

Document in APPARATUS + command: four paths async; south mark joins only sealed paths.

