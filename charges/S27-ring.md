# S27 — circular ring schedule (additive)
**improper:** TwinGlass treated as a second ten-pod swarm; calendar wake; fill-ten; Drive notebooks as SSOT; meters copied into the pack.
**other-track:** TwinGlass is the gait of a turn. The ring schedules `LIVE_NAME`. Sleep writes a packet. Wake on quotes + referee + width. One process.
## Do
1. Add `RING.md`. Do not delete `CAP: 10`.
2. Board: `LIVE_NAME`, `DOOR`, `PACKETS`.
3. Sleep drops live walkers. Wake is `workflow.agent()` / `session/new` with packet fields, empty history.
4. Pointer hygiene: Drive is past. Meaning is `llmve-meaning` VERSION. Do not copy T0–T3, Exergy, or pollutants.
## Do not
- Merge S25 auto-agent.
- Run `llmve-factor-compute`.
- Mint Ω.
- Fuse ACP desk.
- Own meet (still `collate-hourglass`).
