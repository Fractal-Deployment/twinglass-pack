# S02 — CLI spawn-lock

**improper:** Engine `assertLegalSpawnNote` can pass while CLI gait still clones on any `cannotFollow`.
**other-track:** Quote `run-apparatus.md` step 3 vs `engine/main-lattice.ts` refuse paths.

## Do

1. Add `commands/cli-spawn-lock.md` — CLI must quote improperEvidence + otherTrackEvidence before spawn; refuse synonym/antithesis.
2. Edit `commands/run-apparatus.md` step 3 to the same lock (not “any cannotFollow”).
3. Board line: `SPAWN_REFUSED` count when lock fires.

## Do not

Mint Ω. Change engine tests that already pass. Twin.

`measured_omega=false`
