# S26 — workflow host (additive)
**improper:** spawn clones the parent transcript; SI smacks the walker back; fill-to-cap as ritual; Drive as SSOT.
**other-track:** legal note → CLI workflow.agent() brief; SI is a gather ledger; meet staggers until axes are named; start from research priorities; CLI ACTIVE typical 25 / hard 30.
## Do
1. Add `twinglass-dispatch` and `twinglass-tracker`. Do not delete existing skills.
2. Prefab `.grok/workflows/twinglass.rhai`.
3. Pointer hygiene: Drive is past. Meaning is `Fractal-Deployment/llmve-meaning` VERSION.
4. Width: keep engine `SENS_CAP` 10. CLI concurrent sessions typical ≤25, hard STOP 30. Start harvest is situation-sized.
## Do not
- Merge S25 auto-agent.
- Run `llmve-factor-compute`.
- Copy LLMVE instruction sets into this pack.
- Mint Ω.
- Delete `CAP: 10` from the board.
