# S10–S12 — Command align (may split into three PRs)

Align `fork-sens.md`, `clone-experiment.md`, `clone-paces.md` with engine spawn lock (improper + other-track). No synonym forks. Volume gates stay on paces.

`measured_omega=false`
