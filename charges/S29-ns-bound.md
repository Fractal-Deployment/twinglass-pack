# S29 — NS bound form (additive)
**improper:** ten standing pods named Exergy/Mythos; 10k as the gait; Proof-team vs Disproof-team as default; invent-green by a seated owner.
**other-track:** NS functionality is still in the machine. The 10 chairs are not. Cousin-transfer, variant-split, consolidator, dead-end bank, post-search verify. Cap 10 = STOP, not a fill roster. Twin leftover only. Default = one walker + negation_map[]. Circle = LIVE_NAME schedule.
## Do
1. Restack `CIRCULAR.md` to the bound table. Do not delete `CAP: 10`.
2. Proof/disproof = “covers the word problem” vs “wrong object,” written on one pad. Not two camps.
3. Cousin Euler = Energy first; Path only when Energy quotes it.
4. Rewriter after Meet = Codex (markers, telos unmoved).
5. Width 2 only as Energy P vs R inside **one** packet.
## Do not
- Stand up ten owner folders.
- Copy T0–T3, Exergy, pollutants.
- Fuse ACP.
- Own meet (still `collate-hourglass`).
- Rewrite engine `SENS` off the clone tally unless a later charge says `SENS := live`.
