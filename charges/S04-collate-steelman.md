# S04 — Collate vs steelman gate

**improper:** Meet defaults to tournament.
**other-track:** Exclusive leftover only after collate.

## Do

1. Skill one-liners: steelman loads only after collate names exclusive leftover.
2. `scripts/collate-steelman-gate.sh` greps the order.
3. Wire gate into PR checks if cheap.

`measured_omega=false`
