# Twinglass engine (TypeScript)

Main lattice (clone **legs**) ≠ 3D diamond (one agent’s **internal critique**).

`train_ok=false · measured_omega=false · G1=OPEN · feeds_omega=false`

| File | Job |
|---|---|
| `main-lattice.ts` | Legs, hard notes → `spawnLegsBurst`, `enterCritiqueDiamond` (others hibernate), `awaiting-meet`, `converge` (debate/synthesis) |
| `diamond-engine.ts` | Octahedron: four equatorial pads, SI+LCD, per-path pending (async equator), session-log hunt throws |
| `diamond3d.ts` | Vertices / poles |
| `integrity-agent.ts` / `lcd-lens.ts` | Monitor occupants |

```bash
node --experimental-strip-types --test engine/diamond-engine.test.ts engine/main-lattice.test.ts
```

Not a dump meter. Not twin. Not battle.
