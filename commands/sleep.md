---
name: sleep
description: Write a ring sleep packet to packets/<name>.md. Workspace file write. Not MCP. Not a standing team.
---
# /sleep
After octahedron on the live pad. `LIVE_NAME` finished a layer. Write a **WORKING** file in the current workspace:

```
packets/<name>.md
```

One file per locked name. Layers live **inside** the file (`layer_index`). Do not make `packets/Energy/` as an owner folder.

The writer is the CLI's ordinary file write (`Write` / edit the workspace). That **is** the kit writer.

Do **not**:
- hunt MCP `filesystem_*` / `search_tool` for a writer
- spawn the neighbor in this session
- copy the parent transcript into the packet
- mint SemVer from sleep
- fill-ten
- start dump-swarm or a second skill on this pad

Packet fields (from `RING.md`):
```
name
coordinate_language    llmve | general | both
plane_tag?             llmve charges only
chart_axes?
word_problem
candidate_form
thesis_expression
negation_map[]
empty_meets[]
why_neighbor
improperEvidence?
otherTrackEvidence?
dump_backed
layer_index
brief
next_intention
```

After write: print the board. `DOOR: sleep`. `PACKETS: n+1`. `LIVE_NAME:` empty (this session). Sleeping packets do not count as live walkers.
Wake is a **new** session: empty history, packet fields only, quotes required. Command `/ring`.
Keep `CAP: 10`. Meet stays `collate-hourglass`.
