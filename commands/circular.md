---
name: circular
description: Circular construct around V. NS methods bound. Not ten standing chairs. Does not own meet.
---
# /circular
Load `CIRCULAR.md`, then `RING.md`, `WORKFLOW_HOST.md`, and `commands/run-apparatus.md`.
This command **does not own meet**. Meet is still `collate-hourglass`.

The NS piece is real. It is not ten standing teams named Exergy/Mythos.
Copy: cousin-transfer, variant-split, consolidator, dead-end bank, post-search verify.
Refuse 10k as the method. Cap 10 = STOP, not a roster to fill.
Default: one walker + `negation_map[]` on one pad, then the ring pushes the neighbor.
Twin leftover only. Cousin Euler = Energy first. Rewriter after Meet = Codex (markers, telos unmoved).

Sleep writes a packet. Wake only on `why_neighbor` + `otherTrackEvidence` + referee.
`CAP: 10` stays. Do not ingest Drive. Do not copy T0-T3, Exergy, or pollutants.
