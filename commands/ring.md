---
name: ring
description: Circular schedule — which locked name is live. Sleep packet. Wake on quotes. Does not own meet.
---
# /ring
Load `RING.md` and `CIRCULAR.md`, then `WORKFLOW_HOST.md` and `commands/run-apparatus.md`.
This command **does not own meet**. Meet is still `collate-hourglass`.
Spawn lock unchanged. Isolation = empty history.
Seats around V. Sleep writes a packet. Wake only on `why_neighbor` + `otherTrackEvidence` + referee. No calendar wake.
`CAP: 10` stays. Live walkers only; sleeping packets do not count.
LLMVE Energy first, width 1. General charges do not mint mythos tags.
Do not ingest Google Drive copies. Live meaning: `Fractal-Deployment/llmve-meaning` VERSION.
