---
name: hand-correct
description: Paste into Grok Build CLI. Find remaining select / hold-a-token / Price-as-master taint. Correct to Find → Maintain (pathing) → Let-fail. Failure points before the act. No traveling seals or stamps. Do not invent meaning.
---

# Hand-correct leftover taint (Grok Build CLI)

You are on the operator machine. **Meaning is already locked.** Do not invent a new chain.

**Correct chain:** Find → **Maintain (pathing structure)** → Let-fail.

- Find: structure appears that can carry the work (Find_M / Find_G).
- Maintain: keep residual / attention / routing geometry so layer-to-layer **matmul** can compute. **Not hold a token.** Tokens express probability from relationships and context.
- Let-fail: non-vital structure may lose under hierarchy of values. Not select. Not causally select.
- Thumb-on-the-scale: hold-a-token, presuppose the outcome, permanent preferred-set lock.
- Price’s Law is **not** the top variable. Shape \(T_{\mathrm{tail}}\times F\) is bookkeeping only. Operable knobs (temp/top-k/top-p) are not Price’s Law.
- Live SSOT: quote `/home/workspace/llmve-meaning/VERSION` on disk, then `meaning/INSTRUCTION_SET.md` + `PATH_CHAIN_FIND_MAINTAIN_LETFAIL.md`. Do not freeze a version number in this skill. The Hold-named path-chain file is not live.

**Charge name:** **liveness** (rule 17). Not liveliness.

## Before any edit

Mint **single-action failure points** for *this* correction (charge liveness). Express them, then Act. They die when the act ends. Do not copy the last mission’s list.

A traveling seal or stamp is not a failure point. Do not write:

- `train_ok` / `measured_omega` / `G1` / `feeds_omega` / `no invent-green` as headers, footers, or skill closes
- never-expire banners (including a standing SUPERSEDED pack)
- a new slogan that replaces those tokens

Honesty is: this command exited non-zero, this path is OPEN, this verify did not run. Put that on **this** step. Fail in the act (`lookup/FAIL_IN_ACT.md`). Do not add Rule 19.

## Hunt (if they exist, change them)

Search **live surfaces you will open** (product START_HERE, live contracts, live meaning, skills). Do not tour dated archive bodies.

```
causally select
Capacity-to-find + capacity-to-hold + capacity-to-causally-select
find, hold, and causally select
find / hold / select
Viability under Price’s Law
Price-shaped working set
Find_M → Hold → Let-fail
Find → Hold → Let-fail
D1.hold
D1.P_L_path
```

Also purge from those live surfaces: traveling seal packs and stamp-theater.

**Dated leftover names (archive — do not tour; do not stamp; stop live pointers from treating them as jurisdiction):**

| Path (under training_orchestrator artifacts, historical) |
|---|
| `LLMVE_INSTRUCTION_SET_20260818.md` |
| `LLMVE_FRESH_SSOT_ROUND_20260818.md` |
| `LLMVE_P_L_VIABILITY_VS_SHAPE_SSOT_20260814.md` |
| `LLMVE_FOURWAY_FORCE_UNDER_LCD_THERMO_20260817.md` |
| `LLMVE_SSOT_PACKAGE_MYTHOS_PRICE_TAU_INFLATION_FRICTION_20260817.md` |
| `LLMVE_OPERATOR_AUTHORITY_SSOT_20260810.md` |
| `LLMVE_INSTRUCTION_SET_20260824_INTERDEPENDENT.md` |
| `scripts/llmve_price/` if the dir still exists (rename after wording, don’t mint) |
| workflow arm `P_L-PRICE` if the name still exists |

Live pointers (`START_HERE.md`, pattern-matching-machine contract, skills) must not teach Hold / causally-select / Price-as-master.

`hold_id` / `hold_m` **sensors** may stay as readouts of pathing cohesion. Do not let those names put “hold a token” back as the middle step. Prefer `D1.maintain` in speech.

## Do not

- Mint a dump meter or close Ω.
- Restore Price’s Law as master.
- Rewrite `PATH_CHAIN_FIND_MAINTAIN_LETFAIL.md` backwards to Hold.
- Attack `~/.grok/sessions`.
- Load lattice + twin on this charge.
- Scorched-earth archive / worktrees / Desktop twins / testing-bucket (copy-never-delete).
- Replace seals with a new traveling pack.

## Board when done

```
CHANGED: [paths]
LEFT_ARCHIVE: [named, not toured]
NOT_FOUND: [paths]
LEFT_FOR_OPERATOR: [if a file is binary/docx or you would be inventing]
RESTATE: Find → Maintain (pathing) → Let-fail. Not hold a token. Not select. Not Price’s Law as master.
```
