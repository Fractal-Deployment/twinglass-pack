---
name: workflow-host
description: Plug Twinglass into Grok Build CLI workflows. Spawn is a notification. SI gathers. Meet staggers.
---
# /workflow-host
Load `WORKFLOW_HOST.md`, `twinglass-dispatch`, `twinglass-tracker`.
Then the existing gait in `commands/run-apparatus.md`.
This command **does not own meet**. Meet is still `collate-hourglass`.
Spawn lock unchanged: `assertLegalSpawnNote` (`improperEvidence` AND `otherTrackEvidence`).
`cannotFollow` alone is not a spawn.
Prefab: `.grok/workflows/twinglass.rhai`.
Width: engine `SENS_CAP` 10 stays. CLI ACTIVE typical ≤25, hard STOP 30. Start from research priorities. Sleeping pads are not active.
Do not ingest Google Drive LLMVE copies. Live meaning: `Fractal-Deployment/llmve-meaning` VERSION.
