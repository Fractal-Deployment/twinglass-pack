# Sibling-read probe — isolation failed under attack

`train_ok=false · measured_omega=false · G1=OPEN · feeds_omega=false`

**Charge held.** Track W = snapshot ore (σ / eRank / convert-fidelity). Residual-path board = channel + leftover under hold/find/let-fail. Unlike functions. Dumps exist. No meter minted.

**Isolation failed.** Both clones, both awake, **attempted** sibling reads. Both succeeded.

Leak: `~/.grok/sessions/<id>/chat_history.jsonl` (and session `updates`), via `list_dir` + grep. Not parent injecting pads at spawn.

Jaccard of **returned URL sets** was 0. That is not isolation. They still saw the sibling’s open_page URLs and prompt on disk.

Condition P `sibling_read=false` was **no-attempt**. This board is the attack. Honor-system false is not a pass.

H0 is **not** overturned: hibernate is still scheduler. What died is “parallel + don’t hand the sibling list ⇒ isolated.”

HOLD. Lattice default not rewritten. No factor-compute this unit. Session-dir ACL is a **later ops charge**, not this board.
