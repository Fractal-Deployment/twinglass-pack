# packets/ — hibernated ring packets (WORKING)
Not court. Not owner folders. Not SemVer.

One file per locked name: `packets/<name>.md`.
`layer_index` is a field inside the file. Do not create `packets/Energy/` as a standing crew.

Write with the CLI's ordinary file write. See `commands/sleep.md` and `RING.md`.
Do not hunt MCP filesystem tools. Do not copy transcripts.
Wake loads **fields**, empty history, quotes required.
