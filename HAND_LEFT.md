# Correct these — dated orch artifacts are archive

Live meaning: quote `llmve-meaning/VERSION` on disk. Path chain is **Find → Maintain (pathing structure) → Let-fail**.

Do not tour dated `artifacts/LLMVE_SSOT_20260818/` bodies. Do not stamp them. Stop live pointers from treating them as jurisdiction.

`hold_id` / `hold_m` **sensors** can stay as *readouts* of pathing cohesion. Do not let their names redefine the middle step as hold-a-token.

Charge name: **liveness**, not liveliness. Failure points before the act. No traveling seals or stamps.
