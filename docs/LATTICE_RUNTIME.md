# Lattice runtime (pointer)

Do **not** duplicate the engine. Occupant is `skills/twinglass-lattice`. Meet occupant is `skills/collate-hourglass`. Steelman is exclusive leftover only.

Diverge only when both are quoted: improperEvidence ∧ otherTrackEvidence. Refuse synonym, assigned antithesis, spawn-because-stuck.

After 2 CI TEST_FAIL product fixes still red: optional split (A) product (B) test-honesty. Converge via collate (debate if exclusive, synthesis if complementary). Never battle as default. Never twin+lattice same charge.
