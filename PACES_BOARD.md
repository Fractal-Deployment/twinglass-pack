# Clone-paces board (live) — locked leftovers

`train_ok=false · measured_omega=false · G1=OPEN · feeds_omega=false`

Not a rerun. Not a dump-meter. Methodology contraction only.

**Restatement:** leftover-on-merit is not a sampler. Greedy / top-p / nucleus live on the Price surface. A paper that collected a sampler is not a viability board.

Live gates: scout 8 before fork; A 12/9 unique vs scout; B 12/11; union 27; Jaccard 1/23; sibling_read=false both; LCD let-fail on sampler-as-ability; no dump-meter minted. Clone C not spawned.

## Residual stream features

Elhage: residual is a **communication channel**. Layers read a linear projection and write a delta. Features are **directions** (superposition); the stream is not a privileged basis.

That is Clone A’s function set: leftover **in the stream** after a write — hold / let-fail against structure (and the preference matrix in \(W\)).

| This is | This is not |
|---|---|
| Channel + deltas + directions | A sampler |
| Logit lens / tuned-lens: unembed the stream at a layer | Dump-meter viability |
| Track-W-shaped snapshot of geometry | Extra tests of reality |

Logit lens **collects** a snapshot of logits. Claiming it is “what the model believes” or a better sampler is LCD **let-fail**. SI: snapshot ≠ leftover-on-merit ≠ dump meter.

## Contrastive decoding

CD (Li et al.): score = expert − amateur (a **different scoring functional**). DoLa is layer-contrast. η / mirostat / min-p still truncate or reshape \(p\).

Unlike units: **changing the functional** ≠ **truncating \(p\)**. Neither is leftover-in-stream. Neither is a dump meter.

Paces Clone B steelmanned decoder-as-viability (CD, DoLa, mirostat, η first four — not the scout list), then killed it on identity: a working beam/nucleus/CD leaderboard is not model viability (Stahlberg empty MAP / Welleck unlikelihood block that join).

LCD already fired: Hugging Face how-to-generate, Wikipedia nucleus, CD leaderboard-as-ability. Blind to prestige.

## Sequential awake isolation

This paces run was **one awake at a time** (interrupt → hibernate). B never saw A’s pad or URL list. Jaccard computed **only at meet**.

That is **not** true parallel. It does **not** settle hibernate A/B:

| | |
|---|---|
| Sequential + no sibling pad | **This board.** Isolation held under a scheduler. |
| Parallel both-awake | `commands/hibernate-ab.md` — still OPEN. |

If the CLI still one-traces both roles in one context, mark **confounded**. Do not drop interrupt as lattice default until that board.

## Ops

Plugin copy `twinglass-pack-2fecf99d` lacked these files. Reinstall from `main`. `GROK_BUILD_CLI.md` now copies `DISCOVERED.md` + `commands/`.

HOLD. Do not mint the dump meter. Do not spawn C.
